CREATE TABLE COMMENTS (
    ID SERIAL PRIMARY KEY,
    USER_ID INT NOT NULL,
    MOVIE_ID INT NOT NULL,
    DESCRIPTION TEXT    
)
;

COPY COMMENTS(USER_ID,MOVIE_ID,DESCRIPTION) 
FROM '/docker-entrypoint-initdb.d/comments_data.csv' 
WITH CSV HEADER
;